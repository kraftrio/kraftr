export namespace HttpTags {
  export const REST_SEQUENCE = 'tags.request-sequence';
  // pipe streams
  export const RESPONSE_PIPE = 'tags.response-stream';

  export const REQUEST_PIPE = 'tags.request-stream';

  export const CONTROLLER = 'tags.controller';
}
